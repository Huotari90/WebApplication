name: Azure CD Pipeline

on:
  push:
    branches:
      - master  
  pull_request:
    branches:
      - master 

jobs:
  build-and-deploy:
    runs-on: windows-latest  

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3  

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0.x'  

    - name: Install Dependencies
      run: dotnet restore

    - name: Run Tests
      run: dotnet test

    - name: Build
      run: dotnet build --configuration Release --no-restore

    - name: Deploy to Azure
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'WebApplicationWinukka '  # Replace with your Azure App Service name
        slot-name: 'production'  
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}  # Set in your repo secrets
        package: '.WebApplication/bin/Release/net8.0'  # Path to your web app project or web deploy package


